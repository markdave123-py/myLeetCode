class Solution(object):
    def strStr(self, haystack, needle):
        """
        :type haystack: str
        :type needle: str
        :rtype: int
        """
        arr = 0
        l = 0
        i = 0
        currf = s[0]

        while i < len(t):
            arr.apend(t[i])
            while l < len(s):
                if s[l] == t[i]:
                    arrr.pop()
                    i += 1
                    l += 1

                else:
                    l += 1
        